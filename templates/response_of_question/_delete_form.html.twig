<form method="post" action="{{ path('app_response_of_question_delete', {'id': response_of_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ response_of_question.id) }}">
    <button class="btn">Delete</button>
</form>
