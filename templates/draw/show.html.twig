{% extends 'base.html.twig' %}

{% block title %}Draw{% endblock %}

{% block body %}
    <h1>Draw</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id draw</th>
                <td>{{ draw.id }}</td>
            </tr>
            {% for question in draw.question %}
                <th>Id question</th>
                <td>{{ question.id }}</td>
                </tr>
                <tr>
                    <th>value Question</th>
                    <td>{{ question.question }}</td>
                </tr>

                <tr>
                    <th>Point question</th>
                    <td>{{ question.point }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_draw_index') }}">back to list</a>

    <a href="{{ path('app_draw_edit', {'id': draw.id}) }}">edit</a>

    {{ include('draw/_delete_form.html.twig') }}
{% endblock %}
